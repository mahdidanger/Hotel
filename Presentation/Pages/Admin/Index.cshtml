@page
@model Hotel.Pages.Admin.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Hotels[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hotels[0].Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hotels[0].Rooms)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Hotels) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Rooms)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.HotelId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.HotelId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.HotelId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
